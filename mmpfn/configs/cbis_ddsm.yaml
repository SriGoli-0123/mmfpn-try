mgm_heads_list: [2,4,8,16,32,64,128,256]
cap_heads_list: [2,4,8,16,32]
image_type: all
features_per_group: 2  # TabPFN-only; ignored by the TabFM backbone
mixer_type: 'MGM+CAP'  # Optional: 'MGM', 'MGM+CAP', 'MoE'
freeze_icl: true  # TabFM: keep the 1.62B-param ICL stage frozen (bf16); false = full fine-tune, needs ~80 GB
